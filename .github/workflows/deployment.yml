name: API FOR Deploymentt

on:
  push:
    branches: [ master ]
jobs:

  curl:
    runs-on: ubuntu-latest
    steps:
    - name: curl
      run: |              
       #curl -i -u mangeshpa:$Token https://api.github.com/KalpanaAddagada/Javawar/deployments
       curl -i -H "Authorization: token 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" \https://api.github.com/user/repos
